name: VM3 Classic Expected Summary Source 260922
on:
  pull_request:
jobs:
  expected-summary:
    name: Summary
    runs-on: ubuntu-latest
    steps:
      - name: Expected producer deliberately fails
        run: |
          echo 'VM3_EXPECTED_GITHUB_ACTIONS_SUMMARY_FAIL_260922'
          exit 1
